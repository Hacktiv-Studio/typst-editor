// src/hooks/useSaveExport.ts (temporary stub - will be replaced in Task 5)
export function useSaveExport() {
  return {
    handleSave: async () => {},
    handleSaveAs: async () => {},
    handleExport: async (_format: 'pdf' | 'png' | 'svg') => {},
  }
}
